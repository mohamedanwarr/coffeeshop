import 'package:flutter/material.dart';
class PastOreders extends StatelessWidget {
  const PastOreders({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
